import { AccordionStateOptions, AccordionState } from '../utils/constants';
declare const useAccordionState: (options?: AccordionStateOptions) => Required<AccordionState>;
export { useAccordionState };
