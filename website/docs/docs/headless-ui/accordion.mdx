---
sidebar_position: 1
---

import CodeBlock from '@theme/CodeBlock';
import source from '!!raw-loader!@site/src/components/HeadlessUI/Accordion';

# Creating Accordion

First, we create an `Accordion` component with `useAccordion`, `useAccordionProvider`,
and `AccordionProvider` exported from the library.

<CodeBlock language="jsx">{source}</CodeBlock>
