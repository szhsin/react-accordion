---
sidebar_position: 3
---

import CodeBlock from '@theme/CodeBlock';
import source from '!!raw-loader!@site/src/components/InitialEntered';
import { transformCodeBlock } from '@site/src/utils';
import InitialEntered from '@site/src/components/InitialEntered';

# Expanding items initially

You could use the `initialEntered` prop of `AccordionItem` to expand items when accordion first mounts.
In the following example, the first and last item are expanded on mount.

<InitialEntered />

<CodeBlock language="jsx">{transformCodeBlock(source)}</CodeBlock>
